<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tamil V – Portfolio</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Outfit:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --ink: #1c1a17;
    --muted: #7a7570;
    --gold: #b8882a;
    --gold-lt: #e8c87a;
    --bg: #faf9f7;
    --surface: #f3f1ec;
    --border: #e4e0d8;
    --white: #ffffff;
    --nav-h: 64px;
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Outfit', sans-serif;
    background: var(--bg);
    color: var(--ink);
    font-size: 15px;
    line-height: 1.6;
  }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0;
    height: var(--nav-h);
    background: rgba(250,249,247,0.92);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 48px;
    z-index: 100;
  }

  .nav-logo {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-weight: 600;
    color: var(--ink);
    letter-spacing: 0.5px;
    text-decoration: none;
  }
  .nav-logo span { color: var(--gold); }

  .nav-links {
    display: flex; gap: 36px; list-style: none;
  }
  .nav-links a {
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .nav-links a:hover, .nav-links a.active { color: var(--ink); }

  /* ── PAGES ── */
  .page {
    display: none;
    min-height: 100vh;
    padding-top: var(--nav-h);
    animation: fadeIn 0.4s ease;
  }
  .page.active { display: block; }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── HERO ── */
  .hero {
    min-height: calc(100vh - var(--nav-h));
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    padding: 80px 80px 80px 80px;
    gap: 60px;
  }

  .hero-text {}
  .hero-eyebrow {
    font-size: 11px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gold);
    font-weight: 500;
    margin-bottom: 16px;
  }
  .hero-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 72px;
    font-weight: 300;
    line-height: 1.0;
    color: var(--ink);
    margin-bottom: 8px;
  }
  .hero-name strong { font-weight: 600; display: block; }
  .hero-role {
    font-size: 16px;
    color: var(--muted);
    font-weight: 300;
    margin-bottom: 32px;
    letter-spacing: 0.3px;
  }
  .hero-desc {
    font-size: 15px;
    color: #504840;
    line-height: 1.8;
    max-width: 440px;
    margin-bottom: 40px;
  }
  .hero-quote {
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px;
    font-style: italic;
    color: var(--gold);
    margin-bottom: 40px;
    letter-spacing: 0.3px;
  }

  .btn-row { display: flex; gap: 16px; flex-wrap: wrap; }
  .btn {
    display: inline-block;
    padding: 12px 28px;
    font-size: 12px;
    letter-spacing: 2px;
    text-transform: uppercase;
    font-weight: 500;
    border-radius: 2px;
    cursor: pointer;
    transition: all 0.2s;
    text-decoration: none;
  }
  .btn-primary { background: var(--ink); color: var(--white); border: 1.5px solid var(--ink); }
  .btn-primary:hover { background: #333; }
  .btn-outline { background: transparent; color: var(--ink); border: 1.5px solid var(--border); }
  .btn-outline:hover { border-color: var(--ink); }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hero-card {
    width: 340px;
    height: 420px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 20px;
    position: relative;
    overflow: hidden;
  }
  .hero-card::before {
    content: '';
    position: absolute; top: 0; left: 0; right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--gold), var(--gold-lt), var(--gold));
  }
  .avatar-circle {
    width: 100px; height: 100px;
    border-radius: 50%;
    background: linear-gradient(135deg, #e8d5a8, #c8a055);
    display: flex; align-items: center; justify-content: center;
    font-family: 'Cormorant Garamond', serif;
    font-size: 36px;
    font-weight: 600;
    color: white;
    letter-spacing: 1px;
  }
  .hero-card-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 24px;
    font-weight: 600;
    color: var(--ink);
  }
  .hero-card-title { font-size: 12px; letter-spacing: 2px; text-transform: uppercase; color: var(--muted); }
  .hero-chips { display: flex; flex-wrap: wrap; justify-content: center; gap: 6px; padding: 0 24px; }
  .chip {
    font-size: 11px;
    padding: 4px 10px;
    border: 1px solid var(--border);
    border-radius: 20px;
    color: var(--muted);
    background: var(--white);
  }

  /* ── SECTION WRAPPER ── */
  .section { padding: 80px 80px; }
  .section-eyebrow {
    font-size: 10px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--gold);
    font-weight: 500;
    margin-bottom: 8px;
  }
  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 44px;
    font-weight: 300;
    color: var(--ink);
    margin-bottom: 48px;
    line-height: 1.1;
  }

  /* ── WORK PAGE ── */
  .filter-bar {
    display: flex; gap: 8px; margin-bottom: 40px; flex-wrap: wrap;
  }
  .filter-btn {
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
    padding: 8px 18px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--muted);
    border-radius: 2px;
    cursor: pointer;
    transition: all 0.2s;
    font-family: 'Outfit', sans-serif;
  }
  .filter-btn:hover, .filter-btn.active { background: var(--ink); color: white; border-color: var(--ink); }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .project-card {
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 4px;
    overflow: hidden;
    transition: transform 0.25s, box-shadow 0.25s;
    cursor: pointer;
  }
  .project-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.08);
  }
  .project-thumb {
    height: 180px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 48px;
    position: relative;
    overflow: hidden;
  }
  .project-thumb .pattern {
    position: absolute; inset: 0;
    opacity: 0.12;
    background-size: 20px 20px;
  }
  .project-thumb .icon { position: relative; z-index: 1; }

  .thumb-uiux    { background: #f0f4ff; }
  .thumb-logo    { background: #fff8f0; }
  .thumb-poster  { background: #f0fff4; }
  .thumb-pack    { background: #fdf0ff; }
  .thumb-social  { background: #fff0f0; }
  .thumb-garment { background: #f0faf8; }

  .project-body { padding: 20px; }
  .project-cat {
    font-size: 10px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--gold);
    font-weight: 500;
    margin-bottom: 6px;
  }
  .project-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 20px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 8px;
  }
  .project-desc { font-size: 13px; color: var(--muted); line-height: 1.6; }

  /* ── ABOUT PAGE ── */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: start;
  }
  .about-bio p { font-size: 15px; color: #504840; line-height: 1.9; margin-bottom: 16px; }
  .about-quote-block {
    border-left: 2px solid var(--gold);
    padding-left: 24px;
    margin: 32px 0;
  }
  .about-quote-block q {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-style: italic;
    color: var(--ink);
    line-height: 1.4;
    font-weight: 300;
  }

  .skills-section { margin-top: 0; }
  .skills-group { margin-bottom: 32px; }
  .skills-group-title {
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 14px;
    font-weight: 500;
  }
  .skill-row { margin-bottom: 12px; }
  .skill-label { font-size: 13px; color: var(--ink); margin-bottom: 5px; display: flex; justify-content: space-between; }
  .skill-pct { color: var(--muted); font-size: 12px; }
  .skill-track { height: 2px; background: var(--border); border-radius: 1px; }
  .skill-fill { height: 100%; background: var(--gold); border-radius: 1px; transition: width 0.8s ease; }

  .tools-wrap { display: flex; flex-wrap: wrap; gap: 8px; }
  .tool-pill {
    font-size: 12px;
    padding: 6px 14px;
    border: 1px solid var(--border);
    border-radius: 20px;
    color: #504840;
    background: var(--white);
  }

  /* ── EDUCATION ── */
  .edu-timeline { display: flex; flex-direction: column; gap: 28px; margin-top: 48px; }
  .edu-item {
    display: flex; gap: 28px; align-items: flex-start;
    padding: 24px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 4px;
  }
  .edu-year {
    font-family: 'Cormorant Garamond', serif;
    font-size: 28px;
    font-weight: 300;
    color: var(--gold);
    min-width: 80px;
    line-height: 1;
    padding-top: 2px;
  }
  .edu-content {}
  .edu-degree {
    font-family: 'Cormorant Garamond', serif;
    font-size: 20px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 4px;
  }
  .edu-school { font-size: 13px; color: var(--gold); font-weight: 500; margin-bottom: 6px; }
  .edu-detail { font-size: 13px; color: var(--muted); }

  /* ── CONTACT PAGE ── */
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: start;
  }
  .contact-info { display: flex; flex-direction: column; gap: 24px; }
  .contact-card {
    display: flex; align-items: center; gap: 16px;
    padding: 20px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 4px;
  }
  .contact-icon {
    width: 40px; height: 40px;
    border-radius: 50%;
    background: var(--surface);
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    flex-shrink: 0;
  }
  .contact-label { font-size: 11px; letter-spacing: 1.5px; text-transform: uppercase; color: var(--muted); margin-bottom: 3px; }
  .contact-val { font-size: 14px; color: var(--ink); font-weight: 400; word-break: break-all; }

  .contact-form { display: flex; flex-direction: column; gap: 18px; }
  .form-group label {
    display: block;
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 8px;
  }
  .form-group input, .form-group textarea {
    width: 100%;
    padding: 12px 16px;
    border: 1px solid var(--border);
    background: var(--white);
    border-radius: 2px;
    font-family: 'Outfit', sans-serif;
    font-size: 14px;
    color: var(--ink);
    outline: none;
    transition: border-color 0.2s;
  }
  .form-group input:focus, .form-group textarea:focus { border-color: var(--gold); }
  .form-group textarea { height: 120px; resize: vertical; }
  .form-note { font-size: 12px; color: var(--muted); margin-top: -8px; }

  /* ── FOOTER ── */
  footer {
    text-align: center;
    padding: 32px;
    border-top: 1px solid var(--border);
    font-size: 12px;
    color: var(--muted);
    letter-spacing: 0.5px;
  }

  /* ── ACHIEVEMENTS PAGE ── */
  .achieve-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-bottom: 48px;
  }
  .achieve-card {
    padding: 28px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 4px;
    position: relative;
    overflow: hidden;
  }
  .achieve-card::after {
    content: '';
    position: absolute; bottom: 0; left: 0;
    width: 100%; height: 2px;
    background: linear-gradient(90deg, var(--gold), transparent);
  }
  .achieve-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 48px;
    font-weight: 300;
    color: var(--gold);
    line-height: 1;
    margin-bottom: 8px;
    opacity: 0.4;
  }
  .achieve-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 8px;
  }
  .achieve-desc { font-size: 13px; color: var(--muted); line-height: 1.65; }

  /* Internship badge */
  .internship-section {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 32px;
    display: flex;
    align-items: center;
    gap: 24px;
    margin-bottom: 48px;
  }
  .intern-badge {
    width: 64px; height: 64px;
    border-radius: 50%;
    background: var(--gold);
    display: flex; align-items: center; justify-content: center;
    font-size: 28px;
    flex-shrink: 0;
  }
  .intern-label { font-size: 10px; letter-spacing: 3px; text-transform: uppercase; color: var(--gold); margin-bottom: 4px; }
  .intern-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 4px;
  }
  .intern-desc { font-size: 13px; color: var(--muted); }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a class="nav-logo" href="#"><span>T</span>amil V</a>
  <ul class="nav-links">
    <li><a href="#" class="active" onclick="showPage('home',this)">Home</a></li>
    <li><a href="#" onclick="showPage('work',this)">Work</a></li>
    <li><a href="#" onclick="showPage('about',this)">About</a></li>
    <li><a href="#" onclick="showPage('achievements',this)">Achievements</a></li>
    <li><a href="#" onclick="showPage('contact',this)">Contact</a></li>
  </ul>
</nav>

<!-- ══ HOME ══ -->
<section class="page active" id="home">
  <div class="hero">
    <div class="hero-text">
      <div class="hero-eyebrow">Graphic & UI Designer · CSE Student</div>
      <h1 class="hero-name">Tamil<strong>V</strong></h1>
      <p class="hero-role">Salem, Tamil Nadu &nbsp;·&nbsp; Available for freelance</p>
      <p class="hero-desc">I'm a designer who turns ideas into visual stories. From sleek UI screens to bold brand identities, I craft designs that don't just look good — they mean something.</p>
      <p class="hero-quote">"Where imagination meets intention."</p>
      <div class="btn-row">
        <button class="btn btn-primary" onclick="showPage('work', document.querySelectorAll('.nav-links a')[1])">View My Work</button>
        <button class="btn btn-outline" onclick="showPage('contact', document.querySelectorAll('.nav-links a')[4])">Get in Touch</button>
      </div>
    </div>
    <div class="hero-visual">
      <div class="hero-card">
        <div class="avatar-circle">TV</div>
        <div class="hero-card-name">Tamil V</div>
        <div class="hero-card-title">Graphic & UI Designer</div>
        <div class="hero-chips">
          <span class="chip">UI Design</span>
          <span class="chip">Branding</span>
          <span class="chip">Illustration</span>
          <span class="chip">Logo Design</span>
          <span class="chip">Figma</span>
          <span class="chip">Photoshop</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ══ WORK ══ -->
<section class="page" id="work">
  <div class="section">
    <div class="section-eyebrow">Selected Projects</div>
    <div class="section-title">My Work</div>

    <div class="filter-bar">
      <button class="filter-btn active" onclick="filterProjects('all', this)">All</button>
      <button class="filter-btn" onclick="filterProjects('ui-ux', this)">UI / UX</button>
      <button class="filter-btn" onclick="filterProjects('logo', this)">Logo & Branding</button>
      <button class="filter-btn" onclick="filterProjects('poster', this)">Posters</button>
      <button class="filter-btn" onclick="filterProjects('illustration', this)">Illustration</button>
    </div>

    <div class="projects-grid" id="projects-grid">

      <div class="project-card" data-cat="ui-ux">
        <div class="project-thumb thumb-uiux">
          <div class="pattern" style="background-image: radial-gradient(#5566ff 1px, transparent 1px);"></div>
          <span class="icon">📱</span>
        </div>
        <div class="project-body">
          <div class="project-cat">UI / UX</div>
          <div class="project-name">Food Delivery App</div>
          <div class="project-desc">Full mobile UI in Figma — user flows, wireframes, and high-fidelity prototypes with seamless navigation.</div>
        </div>
      </div>

      <div class="project-card" data-cat="ui-ux">
        <div class="project-thumb thumb-uiux">
          <div class="pattern" style="background-image: radial-gradient(#5566ff 1px, transparent 1px);"></div>
          <span class="icon">💻</span>
        </div>
        <div class="project-body">
          <div class="project-cat">UI / UX · Internship</div>
          <div class="project-name">UI/UX Internship Project</div>
          <div class="project-desc">Hands-on real-world UI/UX design during internship — user research, wireframing, and final prototype delivery.</div>
        </div>
      </div>

      <div class="project-card" data-cat="logo">
        <div class="project-thumb thumb-logo">
          <div class="pattern" style="background-image: radial-gradient(#c8922a 1px, transparent 1px);"></div>
          <span class="icon">✦</span>
        </div>
        <div class="project-body">
          <div class="project-cat">Logo & Branding</div>
          <div class="project-name">Local Business Brand Identity</div>
          <div class="project-desc">Complete brand system — logo, color palette, typography, and print-ready brand guidelines.</div>
        </div>
      </div>

      <div class="project-card" data-cat="logo">
        <div class="project-thumb thumb-pack">
          <div class="pattern" style="background-image: radial-gradient(#9944cc 1px, transparent 1px);"></div>
          <span class="icon">📦</span>
        </div>
        <div class="project-body">
          <div class="project-cat">Product & Packaging</div>
          <div class="project-name">Organic Skincare Packaging</div>
          <div class="project-desc">Label design, box dieline, and production-ready packaging files for an organic skincare concept.</div>
        </div>
      </div>

      <div class="project-card" data-cat="poster">
        <div class="project-thumb thumb-poster">
          <div class="pattern" style="background-image: radial-gradient(#22aa66 1px, transparent 1px);"></div>
          <span class="icon">🎨</span>
        </div>
        <div class="project-body">
          <div class="project-cat">Poster Design</div>
          <div class="project-name">College Tech Fest Campaign</div>
          <div class="project-desc">Cohesive social media campaign — Instagram posts, stories, banners, and event posters.</div>
        </div>
      </div>

      <div class="project-card" data-cat="illustration">
        <div class="project-thumb thumb-social">
          <div class="pattern" style="background-image: radial-gradient(#cc4444 1px, transparent 1px);"></div>
          <span class="icon">🖌️</span>
        </div>
        <div class="project-body">
          <div class="project-cat">Illustration</div>
          <div class="project-name">Editorial Illustrations</div>
          <div class="project-desc">Custom illustrations for digital campaigns and editorial content using Adobe Illustrator.</div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- ══ ABOUT ══ -->
<section class="page" id="about">
  <div class="section">
    <div class="section-eyebrow">Who I Am</div>
    <div class="section-title">About Me</div>

    <div class="about-grid">
      <div class="about-bio">
        <p>I'm Tamil V — a graphic and UI designer driven by curiosity and a love for visual storytelling. Every project I touch is shaped by a deep passion for clean aesthetics, thoughtful layouts, and designs that truly connect with people.</p>
        <p>Currently pursuing my B.E computer Science engineering at Bannari Amman Institute o f Technology, I bring a unique intersection of creative design thinking and analytical problem-solving to every project.</p>
        <p>I'm proficient across the Adobe Creative Suite, Figma, CorelDRAW, and I'm constantly exploring new tools like Blender and DaVinci Resolve to expand my creative toolkit.</p>
        <div class="about-quote-block">
          <q>Good design is felt, not just seen.</q>
        </div>

        <div class="skills-group" style="margin-top:32px;">
          <div class="skills-group-title">Design Skills</div>
          <div class="skill-row">
            <div class="skill-label">Graphic Design <span class="skill-pct">90%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:90%"></div></div>
          </div>
          <div class="skill-row">
            <div class="skill-label">UI / UX Design <span class="skill-pct">88%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:88%"></div></div>
          </div>
          <div class="skill-row">
            <div class="skill-label">Logo & Branding <span class="skill-pct">85%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:85%"></div></div>
          </div>
          <div class="skill-row">
            <div class="skill-label">Illustration <span class="skill-pct">80%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:80%"></div></div>
          </div>
        </div>
      </div>

      <div class="skills-section">
        <div class="skills-group">
          <div class="skills-group-title">Tools & Software</div>
          <div class="tools-wrap">
            <span class="tool-pill">Adobe Photoshop</span>
            <span class="tool-pill">Adobe Illustrator</span>
            <span class="tool-pill">Adobe XD</span>
            <span class="tool-pill">Adobe InDesign</span>
            <span class="tool-pill">Figma</span>
            <span class="tool-pill">CorelDRAW</span>
            <span class="tool-pill">Blender</span>
          </div>
        </div>

        <div class="skills-group" style="margin-top:32px;">
          <div class="skills-group-title">Expertise Areas</div>
          <div class="tools-wrap">
            <span class="tool-pill">UI Design</span>
            <span class="tool-pill">Logo Design</span>
            <span class="tool-pill">Poster Design</span>
            <span class="tool-pill">Illustration</span>
            <span class="tool-pill">Branding</span>
            <span class="tool-pill">Product & Packaging</span>
            <span class="tool-pill">Flyer Design</span>
            <span class="tool-pill">Typography</span>
            <span class="tool-pill">Social Media Graphics</span>
            <span class="tool-pill">Garment Design</span>
          </div>
        </div>

        <div class="skills-group" style="margin-top:32px;">
          <div class="skills-group-title">Languages</div>
          <div class="skill-row">
            <div class="skill-label">Tamil <span class="skill-pct">100%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:100%"></div></div>
          </div>
          <div class="skill-row">
            <div class="skill-label">English <span class="skill-pct">80%</span></div>
            <div class="skill-track"><div class="skill-fill" style="width:80%"></div></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Education -->
    <div style="margin-top: 64px;">
      <div class="section-eyebrow">Academic Background</div>
      <div class="section-title" style="margin-bottom:0; font-size:36px;">Education</div>
      <div class="edu-timeline">
        <div class="edu-item">
          <div class="edu-year">2025</div>
          <div class="edu-content">
            <div class="edu-degree">Bachelor of Engineering – Computer Science</div>
            <div class="edu-school">Bannari Amman Institute of Technology, Erode</div>
            <div class="edu-detail">2025 – 2028 &nbsp;·&nbsp; Currently Pursuing</div>
          </div>
        </div>
    </div>
</section>

<!-- ══ ACHIEVEMENTS ══ -->
<section class="page" id="achievements">
  <div class="section">
    <div class="section-eyebrow">Milestones</div>
    <div class="section-title">Achievements</div>

    <!-- Internship highlight -->
    <div class="internship-section">
      <div class="intern-badge">🎓</div>
      <div>
        <div class="intern-label">Certified Internship</div>
        <div class="intern-title">UI/UX Design Internship</div>
        <div class="intern-desc">Successfully completed a hands-on UI/UX internship — gaining real-world experience in user research, wireframing, prototyping, and delivering production-ready design solutions.</div>
      </div>
    </div>

    <div class="achieve-grid">
      <div class="achieve-card">
        <div class="achieve-num">01</div>
        <div class="achieve-title">Adobe Creative Suite Mastery</div>
        <div class="achieve-desc">Completed advanced training across Photoshop, Illustrator, XD, and InDesign with a hands-on project portfolio spanning print and digital.</div>
      </div>
      <div class="achieve-card">
        <div class="achieve-num">02</div>
        <div class="achieve-title">Personal Portfolio Website</div>
        <div class="achieve-desc">Self-built and deployed a personal design portfolio at tamil.netlify.app showcasing UI, branding, and illustration projects.</div>
      </div>
      <div class="achieve-card">
        <div class="achieve-num">03</div>
        <div class="achieve-title">College Design Club</div>
        <div class="achieve-desc">Actively contributing to the college design club — creating event posters, social content, and branding collaterals for various campus events.</div>
      </div>
      
      <div class="achieve-card">
        <div class="achieve-num">05</div>
        <div class="achieve-title">AI-Driven Design Exploration</div>
        <div class="achieve-desc">Actively exploring AI-driven design tools and their integration with traditional graphic design workflows as part of B.E CSE coursework.</div>
      </div>
      <div class="achieve-card">
        <div class="achieve-num">06</div>
        <div class="achieve-title">Multi-Domain Design Skills</div>
        <div class="achieve-desc">Built expertise across 10+ design disciplines — from garment design and packaging to social media campaigns and typography.</div>
      </div>
    </div>
  </div>
</section>

<!-- ══ CONTACT ══ -->
<section class="page" id="contact">
  <div class="section">
    <div class="section-eyebrow">Let's Connect</div>
    <div class="section-title">Get in Touch</div>

    <div class="contact-grid">
      <div class="contact-info">
        <div class="contact-card">
          <div class="contact-icon">📞</div>
          <div>
            <div class="contact-label">Phone</div>
            <div class="contact-val">+91 9345318217</div>
          </div>
        </div>
        <div class="contact-card">
          <div class="contact-icon">✉️</div>
          <div>
            <div class="contact-label">Email</div>
            <div class="contact-val">endless.dreams.43@gmail.com</div>
          </div>
        </div>
        <div class="contact-card">
          <div class="contact-icon">📍</div>
          <div>
            <div class="contact-label">Location</div>
            <div class="contact-val">Salem, Tamil Nadu, India</div>
          </div>
        </div>
        <div class="contact-card">
          <div class="contact-icon">🌐</div>
          <div>
            <div class="contact-label">Portfolio</div>
            <div class="contact-val"><a href="" target="_blank" style="color: var(--gold); text-decoration:none;"></a></div>
          </div>
        </div>
        <div class="contact-card">
          <div class="contact-icon">in</div>
          <div>
            <div class="contact-label">LinkedIn</div>
            <div class="contact-val"><a href="www.linkedin.com/in/tamil-v-402421358" target="_blank" style="color: var(--gold); text-decoration:none;">www.linkedin.com/in/tamil-v-402421358</a></div>
          </div>
        </div>
      </div>

      <div>
        <div class="contact-form">
          <div class="form-group">
            <label>Your Name</label>
            <input type="text" placeholder="e.g. Ravi Kumar">
          </div>
          <div class="form-group">
            <label>Email Address</label>
            <input type="email" placeholder="you@example.com">
          </div>
          <div class="form-group">
            <label>Subject</label>
            <input type="text" placeholder="Project inquiry, Collaboration...">
          </div>
          <div class="form-group">
            <label>Message</label>
            <textarea placeholder="Tell me about your project..."></textarea>
          </div>
          <p class="form-note">* This form is a demo. Use the email or phone above to reach me directly.</p>
          <button class="btn btn-primary" style="align-self:flex-start;">Send Message</button>
        </div>
      </div>
    </div>
  </div>
</section>

<footer>
  © 2025 Tamil V &nbsp;·&nbsp; Graphic & UI Designer &nbsp;·&nbsp; Salem, Tamil Nadu
</footer>

<script>
  function showPage(id, link) {
    document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
    document.querySelectorAll('.nav-links a').forEach(a => a.classList.remove('active'));
    document.getElementById(id).classList.add('active');
    if (link) link.classList.add('active');
    window.scrollTo(0, 0);
  }

  function filterProjects(cat, btn) {
    document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    document.querySelectorAll('.project-card').forEach(card => {
      if (cat === 'all' || card.dataset.cat === cat) {
        card.style.display = 'block';
      } else {
        card.style.display = 'none';
      }
    });
  }
</script>
</body>
</html>
